const eventsService = {
  listEvents: async (_filters) => {
    // Placeholder — DB queries
    return {
      data: [],
      total: 0,
      note: "Query API scaffold — no DB yet",
    };
  },
};

module.exports = { eventsService };
