const eventsService = {
  acceptEvent: async (event) => {
    // Placeholder for now:
    // Later it will insert into PostgreSQL.
    // Here we keep side effects minimal.
    return;
  },
};

module.exports = { eventsService };
